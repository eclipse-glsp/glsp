# Root config files (eslint.config.mjs, .prettierrc, tsconfig.json) and the CLI's .mocharc.json
# resolve @eclipse-glsp/*-config workspace packages and root-level test tooling (mocha, chai, ...)
# without declaring direct dependencies. Hoisting keeps them resolvable yarn-classic-style.
shamefully-hoist=true
